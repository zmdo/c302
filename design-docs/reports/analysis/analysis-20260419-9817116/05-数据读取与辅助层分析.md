# 05 - 数据读取与辅助层分析

> **报告时间：** 2026-04-19 | **分支：** `refactor/f20260419` | **报告人：** Copilot

## 数据读取层

### 目录结构

```
readers/                     862 行
├── __init__.py        (79)  — 读取器注册表 + get_reader()
├── base.py           (205)  — ConnectomeDataReader 基类
├── csv_reader.py     (120)  — CSV 格式读取器
├── openworm.py       (134)  — OpenWorm 项目数据读取器
├── spreadsheet.py    (156)  — 经典电子表格读取器（默认）
├── updated.py         (84)  — 更新版电子表格读取器
└── white.py           (84)  — White et al. 数据读取器
```

### 7 种数据读取器

| 读取器 | 数据源 | 说明 |
|--------|--------|------|
| SpreadsheetDataReader | CElegansNeuronTables.xls | 经典连接组数据（默认） |
| UpdatedSpreadsheetDataReader | NeuronConnectFormatted.xlsx | 更新版 |
| UpdatedSpreadsheetDataReader2 | 同上（不同解析） | 进一步更新 |
| OpenWormReader | owmeta/cect | OpenWorm 项目 API |
| White_whole | White et al. | 完整成体连接组 |
| White_A | White et al. | 成体连接组 |
| White_L4 | White et al. | L4 幼虫连接组 |

### 读取器接口

```python
class ConnectomeDataReader:
    def read_data(self, include_nonconnected: bool = False) -> dict:
        """返回: {
            'cells': list[str],          # 神经元名称
            'conns': list[ConnectionInfo], # 连接信息
            'muscles': list[str],        # 肌肉名称
        }"""
```

---

## 仿真层

```
simulation/                  158 行
├── __init__.py         (0)
└── runner.py         (158)  — run_simulation() + CLI 参数解析
```

`runner.py` 封装 pyNeuroML 的仿真执行，支持 jNeuroML / jNeuroML_NEURON 后端。

---

## 可视化层

```
visualization/               993 行
├── __init__.py         (0)
├── traces.py         (348)  — 膜电位时序图
├── heatmap.py        (205)  — 膜电位热力图
├── connectivity.py   (240)  — 连接矩阵
└── graph.py          (200)  — 网络拓扑图
```

| 模块 | 输出 | 依赖 |
|------|------|------|
| traces.py | 多细胞膜电位折线图 | matplotlib |
| heatmap.py | 时间×细胞 热力矩阵 | matplotlib, numpy |
| connectivity.py | 连接权重矩阵图 | matplotlib, pyNeuroML |
| graph.py | 网络拓扑有向图 | matplotlib |

---

## 工具层

```
utils/                       146 行
├── __init__.py         (0)
└── helpers.py        (146)  — 通用工具函数
```

`helpers.py` 提供：
- `get_str_from_exponential()` — 科学计数法转换
- `load_data_reader()` — 按名称加载数据读取器
- 其他字符串/路径处理工具

---

## CLI

```
cli.py                       156 行
```

基于 `argparse` 的命令行接口：
- `c302 generate` — 生成网络
- `c302 run` — 运行仿真
- `c302 list` — 列出可用参数集和配置
