# 02 - 代码量对比

## factory.py 拆分前后

### 拆分前（单文件）

```
parameters/factory.py  ：1,877 行，11 个类，72 个方法
```

### 拆分后（子包）

```
parameters/factory/
├── __init__.py      73 行  — create_model() 入口 + _LEVEL_TO_CLASS 映射
├── base.py         387 行  — _ModelBase 基类 + _GradedSynapse2Mixin
├── iaf.py          125 行  — _IafModel / _IafActivityModel / _BC1Model
├── hh.py           118 行  — _HHModel / _HHC1Model
├── hh_multi.py     150 行  — _HHMultiCompModel / _HHGradedModel
└── special.py      636 行  — _HHC0Model / _C2Model / _W2DModel
─────────────────────────────
合计               1,489 行
```

**净减少：388 行（−20.7%）**

### 逐文件职责

| 文件 | 类 | 说明 |
|------|------|------|
| `__init__.py` | — | 公共 API `create_model(level)`，按层级名分派到具体工厂类 |
| `base.py` | `_ModelBase`, `_GradedSynapse2Mixin` | 公共生物参数定义、共享突触创建方法、GradedSynapse2 注册逻辑 |
| `iaf.py` | `_IafModel`, `_IafActivityModel`, `_BC1Model` | IAF 细胞族（Level A → B → BC1 继承链） |
| `hh.py` | `_HHModel`, `_HHC1Model` | HH 单腔室族（Level C → C1） |
| `hh_multi.py` | `_HHMultiCompModel`, `_HHGradedModel` | HH 多腔室族（Level D → D1） |
| `special.py` | `_HHC0Model`, `_C2Model`, `_W2DModel` | 独立模型（C0、C2、W2D 无法归入上述继承链） |

## 参数层总量对比

| 维度 | 计划 4 | 计划 5 | 变化 |
|------|--------|--------|------|
| **parameters/ 总行数** | 3,202 | 2,815 | **−12.1%** |
| **parameters/ 文件数** | 7 | 12 | +5（factory/ 子包拆分） |
| **最大单文件** | 1,877（factory.py） | 636（special.py） | **−66.1%** |

## 全项目总量

```
源码  (project_r1/c302/)  ：52 文件，8,776 行（上次 9,174 行，−4.3%）
测试  (project_r1/tests/) ：34 文件，5,591 行（上次 5,418 行，+3.2%）
```
