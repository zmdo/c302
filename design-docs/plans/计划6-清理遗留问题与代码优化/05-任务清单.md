# 05 - 任务清单

## 阶段 1：拆分 `special.py`

- [x] 1.1 创建 `c302/parameters/factory/c2.py`，迁入 `_C2Model` 类及其所有依赖导入
- [x] 1.2 创建 `c302/parameters/factory/w2d.py`，迁入 `_W2DModel` 类及其所有依赖导入
- [x] 1.3 删除 `c302/parameters/factory/special.py`
- [x] 1.4 更新 `factory/__init__.py` 导入：`special` → `c2` + `w2d`
- [x] 1.5 运行全量测试验证拆分无回归

## 阶段 2：`type` → `conn_type` 重命名

- [x] 2.1 `base.py` 中 4 处方法签名和方法体 `type` → `conn_type`
- [x] 2.2 `c2.py` 中 3 处方法签名和方法体 `type` → `conn_type`
- [x] 2.3 `w2d.py` 中 3 处方法签名和方法体 `type` → `conn_type`
- [x] 2.4 `model.py` 中 `get_syn()` 方法签名和方法体 `type` → `conn_type`
- [x] 2.5 运行全量测试验证重命名无回归

## 阶段 3：优化 `_auto_import()`

- [x] 3.1 `configs/__init__.py` 添加 `logger` + `_imported` 哨兵
- [x] 3.2 修改 `_auto_import()` 函数：幂等保护 + `logger.debug` 替代 `pass`
- [x] 3.3 运行全量测试验证优化无回归

## 阶段 4：提升 `_LEVEL_TO_CLASS`

- [x] 4.1 `factory/__init__.py` 中 `_LEVEL_TO_CLASS` 从函数体提升到模块级
- [x] 4.2 运行全量测试验证提升无回归

## 阶段 5：收尾

- [x] 5.1 使用 `gen_index.py --write` 更新所有受影响文件的文件头索引
- [x] 5.2 使用 `check_index.py` 验证所有文件头索引正确
- [x] 5.3 运行全量回归测试（期望 1472 passed, 36 xfailed）
- [x] 5.4 提交并推送到 `origin/refactor/f20260419`
