# 05 - 任务清单

> 执行任务时每完成一项请勾选对应复选框。

---

## 阶段一：分支准备

- [x] 1.1 从 `refactor/f20160416` 创建新分支 `refactor/f20260419`
- [x] 1.2 确认基准测试通过：`pytest project_r1/tests/ -q` → 1285 passed, 36 xfailed, 162 xpassed

## 阶段二：factory 子包骨架

> **增量迁移策略**：`factory/__init__.py` 的 `create_model()` 在阶段 3-6 中增量编写。每完成一个子模块，在 `create_model()` 中添加对应的延迟导入和映射条目，同时保留旧 `factory.py`。阶段七删除旧文件后，`create_model()` 才包含全部 10 个层级映射。

- [x] 2.1 创建 `project_r1/c302/parameters/factory/` 目录
- [x] 2.2 编写 `factory/__init__.py` 初始版本（仅 `create_model()` 函数骨架，`_LEVEL_TO_CLASS` 先为空 dict；旧 `factory.py` 保留不动）
- [x] 2.3 编写 `factory/base.py`（`_ModelBase` + 类属性协议 + `_build_syn_from_params` / `_build_elec_syn_from_params` 模板方法 + `_GradedSynapse2Mixin`）
- [x] 2.4 运行 `from c302.parameters.factory import create_model` 验证导入链

## 阶段三：迁移 IAF 族（A/B/BC1）

- [x] 3.1 编写 `factory/iaf.py`，迁移 `_IafModel`、`_IafActivityModel`、`_BC1Model`
  - [x] 3.1.1 `_IafModel`：保留 `create_models`、`create_generic_*_cell`、`create_offset`、`get_elec_syn`；设置 `_elec_syn_cls = ExpTwoSynapse`，`create_neuron_to_neuron_syn` / `create_neuron_to_muscle_syn` 继承模板方法
  - [x] 3.1.2 `_IafActivityModel`：覆盖 `create_generic_*_cell`（`IafActivityCell`），设置 `_elec_syn_cls = GapJunction`
  - [x] 3.1.3 `_BC1Model`：设置 `_exc_syn_cls = GradedSynapse`，`_exc_param_fields = ("conductance", "erev", "delta", "vth", "k")`
- [x] 3.2 在 `factory/__init__.py` 的 `create_model()` 中添加 A/B/BC1 的延迟导入和 `_LEVEL_TO_CLASS` 条目
- [x] 3.3 运行等价性测试 A/B/BC1 子集，确认通过

## 阶段四：迁移 HH 族（C/C0/C1）

- [x] 4.1 编写 `factory/hh.py`，迁移 `_HHModel`、`_HHC0Model`、`_HHC1Model`
  - [x] 4.1.1 `_HHModel`：保留 `create_models`、`create_generic_*_cell`（`create_hh_cell`）、`create_offsetcurrent_concentrationmodel`、`get_elec_syn`
  - [x] 4.1.2 `_HHC0Model`：**修正继承为 `_GradedSynapse2Mixin, _HHModel`**；仅覆盖 `create_generic_*_cell`（`create_c0_cell`）；设置类属性 `_exc_syn_cls = GradedSynapse2`
  - [x] 4.1.3 `_HHC1Model`：设置 `_exc_syn_cls = GradedSynapse`
- [x] 4.2 在 `factory/__init__.py` 的 `create_model()` 中添加 C/C0/C1 的延迟导入和 `_LEVEL_TO_CLASS` 条目
- [x] 4.3 运行等价性测试 C/C0/C1 子集，确认通过

## 阶段五：迁移 HH 多隔室族（D/D1）

- [x] 5.1 编写 `factory/hh_multi.py`，迁移 `_HHMultiCompModel`、`_HHGradedModel`
  - [x] 5.1.1 `_HHMultiCompModel`：保留 `create_models`、`create_neuron_cell`
  - [x] 5.1.2 `_HHGradedModel`：继承 `_GradedSynapse2Mixin, _HHMultiCompModel`；设置类属性
- [x] 5.2 在 `factory/__init__.py` 的 `create_model()` 中添加 D/D1 的延迟导入和 `_LEVEL_TO_CLASS` 条目
- [x] 5.3 运行等价性测试 D/D1 子集，确认通过

## 阶段六：迁移特殊模型（C2/W2D）

- [x] 6.1 编写 `factory/special.py`，迁移 `_C2Model`、`_W2DModel`
  - [x] 6.1.1 `_C2Model`：原样迁移全部 12 个方法
  - [x] 6.1.2 `_W2DModel`：设置 `_exc_syn_cls = OutputSynapse`，`_inh_syn_cls = OutputSynapse`；保留 `get_exc_syn` / `get_inh_syn` override
- [x] 6.2 在 `factory/__init__.py` 的 `create_model()` 中添加 C2/W2D 的延迟导入和 `_LEVEL_TO_CLASS` 条目
- [x] 6.3 运行等价性测试 C2/W2D 子集，确认通过

## 阶段七：删除旧文件 + 全量验证

- [x] 7.1 删除 `project_r1/c302/parameters/factory.py`（重命名为 `_factory_legacy.py`，待全量测试通过后删除）
- [x] 7.2 更新 `project_r1/c302/parameters/__init__.py` 文件头注释
- [x] 7.3 运行全量测试：`pytest project_r1/tests/ -q` → 确认 passed ≥ 1285, 0 FAILED
- [x] 7.4 运行等价性测试单独确认：`pytest project_r1/tests/test_equivalence.py -q`

## 阶段八：xfail 粒度细化

- [x] 8.1 修改 `test_equivalence.py`：删除 `_mark_known_diffs()`，新增 `BASELINE_PARAMS` 和 `BASELINE_PARAMS_VALUES`
- [x] 8.2 所有结构类测试方法的 `@parametrize` 改用 `BASELINE_PARAMS`
- [x] 8.3 `test_values_match` + `test_count_exact`（TestPopulations）+ `test_total_exact`（TestConnections）使用 `BASELINE_PARAMS_VALUES`（Muscles 有 3 个方法实际失败，Oscillator 仅 `test_values_match` 失败）
- [x] 8.4 运行等价性测试，确认 xpassed = 0

## 阶段九：configs 自动发现

- [x] 9.1 修改 `configs/__init__.py`：`_auto_import()` 改为 `pkgutil.iter_modules` 扫描
- [x] 9.2 运行 `python -c "from c302.configs import list_configs; print(list_configs())"` 确认 16 个配置可发现
- [x] 9.3 运行全量测试确认无回归

## 阶段十：补全 factory 单元测试

- [x] 10.1 新增 `TestIafModel`（A 级）：`create_models`、`create_generic_neuron_cell`、`create_neuron_to_neuron_syn`、`get_elec_syn`
- [x] 10.2 新增 `TestIafActivityModel`（B 级）：`create_generic_neuron_cell`（`IafActivityCell`）、`get_elec_syn`（`GapJunction`）
- [x] 10.3 新增 `TestBC1Model`（BC1 级）：`create_neuron_to_neuron_syn`（`GradedSynapse`）、`get_exc_syn`、`get_inh_syn`
- [x] 10.4 新增 `TestHHModel`（C 级）：`create_models`、`create_generic_neuron_cell`、`create_offsetcurrent_concentrationmodel`
- [x] 10.5 新增 `TestC2Model`（C2 级）：`create_models`、`create_generic_muscle_cell`、`get_exc_syn`（多态）、`get_inh_syn`、`create_muscle_to_muscle_syn`、`is_analog_conn`、`is_elec_conn`
- [x] 10.6 新增 `TestW2DModel`（W2D 级）：`create_models`、`create_generic_neuron_cell`（`CellW2D`）、`get_exc_syn`（`OutputSynapse`）
- [x] 10.7 运行全量测试确认全部通过

## 阶段十一：收尾

- [x] 11.1 代码风格自查（后端符合 SP-CODE-2026-001）
- [x] 11.2 Python 注释自查（函数体关键逻辑、条件分支、循环、异常处理必须有行内注释）
- [x] 11.3 后端单元测试自查（每个新增/修改的 Python 模块必须有对应 tests/test_*.py）
- [x] 11.4 运行 pytest 确认所有测试通过
- [x] 11.5 确认最终指标：最大单文件 ≤ 400 行（`_C2Model` 所在文件除外）、factory 总行数 ≤ 1,200 行
- [x] 11.6 版本号 commit 递增
- [x] 11.7 Git 提交并推送
