# 04 - 实施细节

## 文件变更清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `design-docs/glossary.md` | 新增 | 领域词汇表（中英文对照） |
| `scripts/verify-comment-only/verify_comment_only.py` | 新增 | 代码等价性验证主脚本 |
| `scripts/verify-comment-only/test_verify.py` | 新增 | 验证脚本的单元测试 |
| `project/c302/ConnectomeReader.py` | 修改（仅注释） | 数据读取层核心，翻译英文注释并补充中文注释 |
| `project/c302/SpreadsheetDataReader.py` | 修改（仅注释） | XLS 读取器注释 |
| `project/c302/UpdatedSpreadsheetDataReader.py` | 修改（仅注释） | CSV 读取器注释 |
| `project/c302/UpdatedSpreadsheetDataReader2.py` | 修改（仅注释） | 修改版 CSV 读取器注释 |
| `project/c302/OpenWormReader.py` | 修改（仅注释） | owmeta 读取器注释 |
| `project/c302/White_whole.py` | 修改（仅注释） | cect 适配器注释 |
| `project/c302/White_A.py` | 修改（仅注释） | cect 适配器注释 |
| `project/c302/White_L4.py` | 修改（仅注释） | cect 适配器注释 |
| `project/c302/bioparameters.py` | 修改（仅注释） | 参数化层基础类注释 |
| `project/c302/parameters_A.py` | 修改（仅注释） | Level A 参数注释 |
| `project/c302/parameters_B.py` | 修改（仅注释） | Level B 参数注释 |
| `project/c302/parameters_C.py` | 修改（仅注释） | Level C 参数注释 |
| `project/c302/parameters_C0.py` | 修改（仅注释） | Level C0 参数注释 |
| `project/c302/parameters_C1.py` | 修改（仅注释） | Level C1 参数注释 |
| `project/c302/parameters_C2.py` | 修改（仅注释） | Level C2 参数注释（1586行，注释量最大） |
| `project/c302/parameters_D.py` | 修改（仅注释） | Level D 参数注释 |
| `project/c302/parameters_D1.py` | 修改（仅注释） | Level D1 参数注释 |
| `project/c302/parameters_BC1.py` | 修改（仅注释） | Level BC1 混合参数注释 |
| `project/c302/parameters_W2D.py` | 修改（仅注释） | Level W2D 发育阶段参数注释 |
| `project/c302/__init__.py` | 修改（仅注释） | 核心网络生成模块，注释量最大（1867行） |
| `project/c302/c302_utils.py` | 修改（仅注释） | 可视化工具注释 |
| `project/c302/runAndPlot.py` | 修改（仅注释） | 仿真编排注释 |
| `project/c302/gen_graph.py` | 修改（仅注释） | 网络图生成注释 |
| `project/c302/CompareMain.py` | 修改（仅注释） | 数据比较注释（48条英文注释需翻译） |
| `project/c302/backers.py` | 修改（仅注释） | 赞助者信息注释 |
| `project/c302/c302_info.py` | 修改（仅注释） | 文档生成注释 |
| `project/c302/analyse.py` | 修改（仅注释） | 基础绘图注释 |
| `project/c302/c302_Full.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_FW.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_IClamp.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_IClampMuscle.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_MultiSyns.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_MuscleTest.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_Muscles.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_MusclesSine.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_Oscillator.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_OscillatorM.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_Pharyngeal.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_RIA.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_Social.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_Syns.py` | 修改（仅注释） | 配置脚本注释 |
| `project/c302/c302_TapWithdrawal.py` | 修改（仅注释） | 配置脚本注释（474行，最大配置脚本） |
| `project/c302/c302_TargetMuscle.py` | 修改（仅注释） | 配置脚本注释 |

---

## 1. 词汇表（`design-docs/glossary.md`）

词汇表为 Markdown 文件，按以下分类组织：

### 格式规范

```markdown
# c302 项目领域词汇表

> 统一翻译词汇，避免同一概念使用不同中文译名。

## 神经科学术语

| 英文术语 | 中文译名 | 说明 | 主要出现模块 |
|---------|---------|------|------------|
| neuron | 神经元 | 神经系统的基本功能单元 | 全局 |
| synapse | 突触 | 神经元间信号传递的连接结构 | ConnectomeReader, __init__ |
| connectome | 连接组 | 神经元间完整连接图谱 | 全局 |
...
```

### 必须收录的词汇分类

1. **神经科学术语**：neuron, synapse, connectome, gap junction, membrane potential, conductance, ion channel, acetylcholine, GABA, glutamate, dopamine, serotonin, capacitance
2. **NeuroML/LEMS 建模术语**：Population, Instance, Projection, ElectricalProjection, ContinuousProjection, Connection, InputList, PulseGenerator, SineGenerator, NeuroMLDocument, Network
3. **电生理术语**：integrate-and-fire (IaF), Hodgkin-Huxley (HH), leak conductance, activation gate, inactivation gate, calcium dynamics, voltage clamp
4. **线虫生物学术语**：C. elegans（秀丽隐杆线虫）, hermaphrodite（雌雄同体）, body wall muscle（体壁肌肉）, pharynx（咽部）, tap withdrawal（触碰退缩）
5. **c302 框架术语**：parameter set（参数层级）, data reader（数据读取器）, configuration script（配置脚本）, syntype（突触类型）, synclass（突触分类）

---

## 2. 验证脚本（`scripts/verify-comment-only/verify_comment_only.py`）

### 完整实现

```python
# =============================================================================
# 功能描述：
#   代码等价性验证工具，用于确认对 Python 源文件的修改仅涉及注释变更。
#   通过去除注释后格式化再比较 SHA-256 哈希值来判断代码是否等价。
#
# 类与方法索引：
#   strip_comments          (L XX) — 去除 Python 源码中的所有注释 token
#   normalize_code          (L XX) — 格式化去注释后的代码（通过 ast.unparse）
#   hash_code               (L XX) — 计算格式化代码的 SHA-256 哈希值
#   verify_file             (L XX) — 验证单个文件对是否仅有注释变更
#   verify_directory        (L XX) — 批量验证目录下所有 .py 文件
#   main                    (L XX) — CLI 入口
#
# 更新日志：
#   2026-04-16  zmdo  初始版本
#
# 当前维护者：zmdo
# =============================================================================
import ast
import hashlib
import io
import os
import sys
import tokenize
from pathlib import Path


def strip_comments(source: str) -> str:
    """去除 Python 源码中所有注释，保留其他所有 token。

    :param source: 原始 Python 源码字符串
    :return: 去除注释后的源码字符串
    :raises tokenize.TokenError: 源码存在词法错误时抛出
    """
    # 使用 tokenize 精确识别注释 token，避免误处理字符串中的 #
    tokens = tokenize.generate_tokens(io.StringIO(source).readline)
    result_tokens = []
    for tok_type, tok_string, tok_start, tok_end, tok_line in tokens:
        # 跳过所有 COMMENT 类型 token，保留其他所有内容
        if tok_type == tokenize.COMMENT:
            continue
        result_tokens.append((tok_type, tok_string, tok_start, tok_end, tok_line))
    return tokenize.untokenize(result_tokens)


def normalize_code(source: str) -> str:
    """通过 ast.parse + ast.unparse 格式化代码，消除空白差异。

    :param source: 去除注释后的 Python 源码字符串
    :return: 格式化后的代码字符串；解析失败时返回原字符串
    """
    try:
        # ast.unparse 将语法树重新序列化，消除注释行造成的空行差异
        return ast.unparse(ast.parse(source))
    except SyntaxError:
        # 解析失败时直接使用去注释后的原始文本
        return source


def hash_code(source: str) -> str:
    """计算代码字符串的 SHA-256 哈希值。

    :param source: 代码字符串
    :return: 十六进制哈希字符串
    """
    # 使用 UTF-8 编码，确保中文字符一致处理
    return hashlib.sha256(source.encode("utf-8")).hexdigest()


def verify_file(original_path: str, modified_path: str) -> bool:
    """验证两个文件是否代码等价（仅注释不同）。

    :param original_path: 原始文件路径
    :param modified_path: 修改后文件路径
    :return: True 表示仅注释变更，False 表示代码被修改
    """
    # 读取两个文件的源码
    original_src = Path(original_path).read_text(encoding="utf-8")
    modified_src = Path(modified_path).read_text(encoding="utf-8")

    # 分别去除注释并格式化
    original_norm = normalize_code(strip_comments(original_src))
    modified_norm = normalize_code(strip_comments(modified_src))

    # 计算哈希值并比较
    original_hash = hash_code(original_norm)
    modified_hash = hash_code(modified_norm)

    if original_hash == modified_hash:
        print(f"[OK]   {os.path.basename(modified_path)} — 代码等价，仅注释变更")
        return True
    else:
        print(f"[FAIL] {os.path.basename(modified_path)} — 代码不等价！")
        print(f"       原始 Hash:  {original_hash}")
        print(f"       修改后 Hash: {modified_hash}")
        # 输出差异行以辅助定位
        orig_lines = original_norm.splitlines()
        mod_lines = modified_norm.splitlines()
        for i, (ol, ml) in enumerate(zip(orig_lines, mod_lines), 1):
            if ol != ml:
                print(f"       首个差异行 {i}:")
                print(f"         原始:   {ol!r}")
                print(f"         修改后: {ml!r}")
                break
        return False


def verify_directory(original_dir: str, modified_dir: str) -> bool:
    """批量验证目录下所有 .py 文件的代码等价性。

    :param original_dir: 原始目录路径
    :param modified_dir: 修改后目录路径
    :return: True 表示全部通过，False 表示存在失败
    """
    all_passed = True
    # 遍历修改后目录中的所有 .py 文件
    for py_file in sorted(Path(modified_dir).rglob("*.py")):
        # 计算原始目录中对应文件的路径
        rel = py_file.relative_to(modified_dir)
        orig_file = Path(original_dir) / rel
        if not orig_file.exists():
            print(f"[SKIP] {rel} — 原始文件不存在，跳过")
            continue
        if not verify_file(str(orig_file), str(py_file)):
            all_passed = False
    return all_passed


def main() -> None:
    """CLI 入口：接收原始路径和修改后路径，执行验证。"""
    if len(sys.argv) != 3:
        print("用法: python verify_comment_only.py <原始文件或目录> <修改后文件或目录>")
        sys.exit(1)

    original_path = sys.argv[1]
    modified_path = sys.argv[2]

    # 根据路径类型选择文件验证还是目录验证
    if os.path.isdir(original_path) and os.path.isdir(modified_path):
        success = verify_directory(original_path, modified_path)
    elif os.path.isfile(original_path) and os.path.isfile(modified_path):
        success = verify_file(original_path, modified_path)
    else:
        print("错误：两个路径必须同为文件或同为目录")
        sys.exit(1)

    sys.exit(0 if success else 1)


if __name__ == "__main__":
    main()
```

### 单元测试（`test_verify.py`）

```python
# =============================================================================
# 功能描述：
#   verify_comment_only.py 的单元测试。
#   覆盖正常注释变更、代码变更检测、空文件、Unicode、语法错误等边界用例。
#
# 类与方法索引：
#   TestStripComments            (L XX) — strip_comments 函数测试
#   TestNormalizeCode            (L XX) — normalize_code 函数测试
#   TestVerifyFile               (L XX) — verify_file 函数测试
#
# 更新日志：
#   2026-04-16  zmdo  初始版本
#
# 当前维护者：zmdo
# =============================================================================
import tempfile
import unittest
from pathlib import Path

from verify_comment_only import hash_code, normalize_code, strip_comments, verify_file


class TestStripComments(unittest.TestCase):
    """测试 strip_comments 函数的各种场景。"""

    def test_removes_inline_comment(self) -> None:
        """验证行内注释被正确去除。"""
        # 行末注释应该被完全移除
        src = "x = 1  # 这是一个注释\n"
        result = strip_comments(src)
        self.assertNotIn("#", result)
        self.assertIn("x = 1", result)

    def test_removes_standalone_comment_line(self) -> None:
        """验证独立注释行被去除。"""
        src = "# 这是独立注释\nx = 1\n"
        result = strip_comments(src)
        self.assertNotIn("这是独立注释", result)
        self.assertIn("x = 1", result)

    def test_preserves_hash_in_string(self) -> None:
        """验证字符串中的 # 不被误删。"""
        # 字符串内的 # 不是注释，必须保留
        src = 'url = "http://example.com/#anchor"\n'
        result = strip_comments(src)
        self.assertIn("#anchor", result)

    def test_empty_file(self) -> None:
        """验证空文件不报错。"""
        result = strip_comments("")
        self.assertEqual(result.strip(), "")


class TestNormalizeCode(unittest.TestCase):
    """测试 normalize_code 函数的格式化一致性。"""

    def test_equivalent_after_comment_removal(self) -> None:
        """验证注释行导致的空行差异不影响归一化结果。"""
        src1 = "x = 1\n\n\ny = 2\n"
        src2 = "x = 1\ny = 2\n"
        # ast.unparse 会消除空行差异，两者归一化结果应相同
        self.assertEqual(normalize_code(src1), normalize_code(src2))


class TestVerifyFile(unittest.TestCase):
    """测试 verify_file 函数的验证逻辑。"""

    def _write_temp(self, content: str) -> Path:
        """创建临时文件并写入内容，返回路径。"""
        f = tempfile.NamedTemporaryFile(mode="w", suffix=".py",
                                        delete=False, encoding="utf-8")
        f.write(content)
        f.close()
        return Path(f.name)

    def test_comment_only_change_passes(self) -> None:
        """仅注释变更时验证应通过。"""
        orig = self._write_temp("x = 1\n")
        mod = self._write_temp("# 新增注释\nx = 1\n")
        self.assertTrue(verify_file(str(orig), str(mod)))

    def test_translate_comment_passes(self) -> None:
        """英文注释翻译为中文时验证应通过。"""
        orig = self._write_temp("x = 1  # set x to 1\n")
        mod = self._write_temp("x = 1  # 将 x 设为 1\n")
        self.assertTrue(verify_file(str(orig), str(mod)))

    def test_code_change_fails(self) -> None:
        """代码变更时验证应失败。"""
        orig = self._write_temp("x = 1\n")
        mod = self._write_temp("x = 2\n")
        self.assertFalse(verify_file(str(orig), str(mod)))

    def test_identical_files_pass(self) -> None:
        """完全相同的文件应通过验证。"""
        orig = self._write_temp("x = 1\ny = 2\n")
        mod = self._write_temp("x = 1\ny = 2\n")
        self.assertTrue(verify_file(str(orig), str(mod)))


if __name__ == "__main__":
    unittest.main()
```

---

## 3. 注释重点区域说明

### `__init__.py` — `generate()` 函数关键注释点

| 代码区域 | 需要注释的内容 |
|---------|-------------|
| NeuroMLDocument 创建 | 说明文档 ID 命名规则和结构作用 |
| 细胞模型加载分支 | 说明各 Level 分支的选择条件（is_level_A/B/C/D） |
| Population + Instance 创建循环 | 说明种群与实例的 1:1 关系和 3D 坐标分配逻辑 |
| 连接遍历主循环 | 说明三类突触（化学/电/模拟）的判断顺序 |
| `connection_number_override` 正则匹配 | 说明权重覆盖机制的匹配规则 |
| `write_to_file()` 文件写出 | 说明输出的两个文件（.net.nml 和 LEMS .xml）的用途 |

### `bioparameters.py` — 参数系统注释点

| 代码区域 | 需要注释的内容 |
|---------|-------------|
| `BioParameter` 类 | 说明各字段含义（name/value/source/certainty） |
| `add_bioparameter()` | 说明参数注册机制 |
| `get_bioparameter()` | 说明查找方式及未找到时的行为 |
| `c302ModelPrototype` 的 `is_level_*()` 方法 | 说明层级判断的业务意义 |
| `get_syn()` | 说明突触 ID 的命名规则 |
| `is_elec_conn()` / `is_analog_conn()` | 说明两种非化学突触的业务背景 |

### `ConnectomeReader.py` — 连接组接口注释点

| 代码区域 | 需要注释的内容 |
|---------|-------------|
| `ConnectionInfo` 类 | 说明各字段含义和有效值枚举 |
| `PREFERRED_NEURON_NAMES` | 说明 302 个神经元名称列表的来源（线虫连接组） |
| `remove_leading_index_zero()` | 说明名称规范化的原因（VB01 → VB1） |
| `is_muscle()` / `is_neuron()` | 说明判断规则 |
| `check_neurons()` | 说明标准名称校验的作用 |
