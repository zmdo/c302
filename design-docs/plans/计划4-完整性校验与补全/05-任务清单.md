# 05 - 任务清单

> 执行任务时每完成一项请勾选对应复选框。

---

## 阶段一：生成参考基线

- [ ] 1.1 编写 `scripts/generate_baselines.py`
  - [ ] 1.1.1 实现 `extract_baseline()` 函数：从 NeuroML 文档提取种群/连接/刺激/参数/细胞/突触计数
  - [ ] 1.1.2 实现主循环：遍历 10 层级 × 8 配置 = 80 种组合（部分可能因原始代码不支持而跳过）
  - [ ] 1.1.3 错误处理：某些组合可能失败（如 FW 仅 W2D/C2 有），记录跳过原因
- [ ] 1.2 运行基线生成脚本
  - [ ] 1.2.1 确认原始代码可运行（`python -m c302.c302_IClamp A` 无报错）
  - [ ] 1.2.2 执行脚本，生成 `project_r1/tests/fixtures/baselines/*.json`
  - [ ] 1.2.3 验证生成的 JSON 文件数量和格式正确
- [ ] 1.3 提交阶段一

## 阶段二：factory.py 拆分

- [ ] 2.1 创建 `project_r1/c302/parameters/custom_types.py`
  - [ ] 2.1.1 从 factory.py 迁移 `IafActivityCell` 类
  - [ ] 2.1.2 从 factory.py 迁移 `GradedSynapse2` 类
  - [ ] 2.1.3 定义 `NonNeuroMLCustomType` 基类（如尚未存在）
- [ ] 2.2 创建 `project_r1/c302/parameters/cell_builder.py`
  - [ ] 2.2.1 从 factory.py 迁移 `_create_hh_cell()` → `create_hh_cell()`
  - [ ] 2.2.2 从 factory.py 迁移 `_create_c0_cell()` → `create_c0_cell()`
- [ ] 2.3 更新 factory.py
  - [ ] 2.3.1 删除已迁移的类和函数定义
  - [ ] 2.3.2 添加从 `custom_types` 和 `cell_builder` 的导入
  - [ ] 2.3.3 确保所有现有测试通过（零行为变更）
- [ ] 2.4 编写 `tests/test_parameters/test_custom_types.py`
  - [ ] 2.4.1 测试 IafActivityCell 创建和 export
  - [ ] 2.4.2 测试 GradedSynapse2 创建和 export
- [ ] 2.5 验证阶段二
  - [ ] 2.5.1 `pytest project_r1/tests/` 全部通过（零回归）
  - [ ] 2.5.2 提交阶段二

## 阶段三：D/D1 集成修通

- [ ] 3.1 排查 D 级生成链路
  - [ ] 3.1.1 运行 `generate("test", get_parameter_set("D"), cells=["ADAL"])` 并记录错误
  - [ ] 3.1.2 修复 `population.py` 中 `cells/` 目录创建（`os.makedirs` + `exist_ok=True`）
  - [ ] 3.1.3 修复 morphology 文件路径解析
  - [ ] 3.1.4 修复 per-neuron `.cell.nml` 文件写出
- [ ] 3.2 排查 D1 级生成链路
  - [ ] 3.2.1 运行 `generate("test", get_parameter_set("D1"), cells=["ADAL"])` 并记录错误
  - [ ] 3.2.2 修复 D1 特有的 GradedSynapse2 突触在连接创建中的注册
- [ ] 3.3 添加 D/D1 等价性测试用例
  - [ ] 3.3.1 将 ("IClamp", "D") 和 ("IClamp", "D1") 加入 SUPPORTED_CASES
  - [ ] 3.3.2 与基线数据比对种群数量、连接数量
- [ ] 3.4 验证阶段三
  - [ ] 3.4.1 D/D1 × IClamp 生成成功且与基线一致
  - [ ] 3.4.2 `pytest project_r1/tests/` 全部通过
  - [ ] 3.4.3 提交阶段三

## 阶段四：BC1 Factory

- [ ] 4.1 在 `custom_types.py` 中确认 `IafActivityCell` 可复用
- [ ] 4.2 在 `factory.py` 中实现 `_BC1Model(_IafActivityModel)`
  - [ ] 4.2.1 实现 `create_neuron_to_neuron_syn()` — GradedSynapse + GapJunction
  - [ ] 4.2.2 实现 `create_neuron_to_muscle_syn()` — GradedSynapse + GapJunction
  - [ ] 4.2.3 实现 `get_exc_syn()` / `get_inh_syn()` — 返回 GradedSynapse
  - [ ] 4.2.4 实现 `get_elec_syn()` — 返回 GapJunction
- [ ] 4.3 更新 `__init__.py`
  - [ ] 4.3.1 将 "BC1" 从 `_PENDING_LEVELS` 移至 `_LEVEL_REGISTRY`
  - [ ] 4.3.2 在 `_LEVEL_TO_CLASS` 中注册 `"BC1": _BC1Model`
- [ ] 4.4 验证 BC1 × 7 配置
  - [ ] 4.4.1 运行生成并与基线比对（如有基线；BC1 不在 test.sh 中，需判断是否生成基线）
  - [ ] 4.4.2 添加 BC1 factory 单元测试
- [ ] 4.5 提交阶段四

## 阶段五：W2D Factory

- [ ] 5.1 在 `custom_types.py` 中新增自定义类型
  - [ ] 5.1.1 实现 `CellW2D` 类
  - [ ] 5.1.2 实现 `OutputSynapse` 类
- [ ] 5.2 在 `factory.py` 中实现 `_W2DModel(_ModelBase)`
  - [ ] 5.2.1 实现 `create_generic_muscle_cell()` / `create_generic_neuron_cell()` — CellW2D
  - [ ] 5.2.2 实现 `create_neuron_to_neuron_syn()` — OutputSynapse + GapJunction
  - [ ] 5.2.3 实现 `create_neuron_to_muscle_syn()` — OutputSynapse + GapJunction
  - [ ] 5.2.4 实现 `create_muscle_to_muscle_syn()` — OutputSynapse + GapJunction
  - [ ] 5.2.5 实现 `get_elec_syn()` / `get_exc_syn()` / `get_inh_syn()`
- [ ] 5.3 在 `model.py` 中新增 `is_level_W2D()` 方法
- [ ] 5.4 更新 `__init__.py`：将 "W2D" 移至 `_LEVEL_REGISTRY`
- [ ] 5.5 修改 `generator/__init__.py` — 添加 W2D 细胞注册分支
- [ ] 5.6 验证 W2D × 8 配置（与基线比对）
- [ ] 5.7 添加 W2D factory + custom_types 单元测试
- [ ] 5.8 提交阶段五

## 阶段六：C2 Factory

- [ ] 6.1 在 `custom_types.py` 中新增 C2 自定义组件类型
  - [ ] 6.1.1 实现 `SwitchedGapJunction`（含 export）
  - [ ] 6.1.2 实现 `DelayedGapJunction`（含 export）
  - [ ] 6.1.3 实现 `ProprioGapJunction`（含 export）
  - [ ] 6.1.4 实现 `ProprioGapJunction2`（含 export）
  - [ ] 6.1.5 实现 `DelayedGradedSynapse`（含 export）
  - [ ] 6.1.6 实现 `NeuronMuscle`（含 export）
  - [ ] 6.1.7 实现 `MuscleConcentrationModel`（含 export）
  - [ ] 6.1.8 实现 `MuscleConcentrationModel2`（含 export）
- [ ] 6.2 在 `factory.py` 中实现 `_C2Model(_HHModel)`
  - [ ] 6.2.1 实现 `create_models()` — 含 `create_offsetcurrent_concentrationmodel` + `create_muscle_to_muscle_syn`
  - [ ] 6.2.2 实现 `create_offsetcurrent_concentrationmodel()` — 双浓度模型
  - [ ] 6.2.3 实现 `create_neuron_to_neuron_syn()` — GradedSynapse + GapJunction + DelayedGapJunction
  - [ ] 6.2.4 实现 `get_elec_syn()` — per-connection 分发（Delayed/Proprio/ProprioGap2/Gap）
  - [ ] 6.2.5 实现 `get_exc_syn()` — per-connection 分发（NeuronMuscle/GradedSynapse2/GradedSynapse）
  - [ ] 6.2.6 实现 `get_inh_syn()` — per-connection 分发
  - [ ] 6.2.7 实现 `create_muscle_to_muscle_syn()` — GapJunction
  - [ ] 6.2.8 实现 `create_n_connection_synapse()` — 扩展支持 C2 自定义类型
  - [ ] 6.2.9 实现 `is_elec_conn()` / `is_analog_conn()` 覆盖
- [ ] 6.3 更新 `__init__.py`：将 "C2" 移至 `_LEVEL_REGISTRY`
- [ ] 6.4 修改 `generator/connection.py` — 支持 muscle_to_muscle 连接类型
- [ ] 6.5 验证 C2 × 8 配置（与基线比对）
- [ ] 6.6 添加 C2 factory + custom_types 单元测试
- [ ] 6.7 提交阶段六

## 阶段七：等价性测试升级

- [ ] 7.1 重写 `tests/test_equivalence.py`
  - [ ] 7.1.1 实现 `load_baseline()` — 从 fixtures/baselines/ 加载 JSON
  - [ ] 7.1.2 参数化 fixture — 自动发现所有可用基线文件
  - [ ] 7.1.3 实现 `TestPopulations.test_count_exact` — 种群数量精确比对
  - [ ] 7.1.4 实现 `TestConnections.test_total_exact` — 连接总数精确比对
  - [ ] 7.1.5 实现 `TestStimuli.test_count_exact` — 刺激数量精确比对（修复 >= 0 恒真）
  - [ ] 7.1.6 实现 `TestBioParameters.test_count_exact` — 参数数量精确比对
  - [ ] 7.1.7 实现 `TestBioParameters.test_values_match` — 逐参数值比对
  - [ ] 7.1.8 实现 `TestCellModels.test_iaf_count` / `test_hh_count` — 精确比对
  - [ ] 7.1.9 实现 `TestSynapseModels` — 各突触类型计数精确比对（exp_two / gap_junction / graded_synapse / graded_synapse2）
- [ ] 7.2 运行完整等价性测试
  - [ ] 7.2.1 确认 72 组合全部通过（或记录已知差异并注释原因）
- [ ] 7.3 提交阶段七

## 阶段八：收尾

- [ ] 8.1 代码风格自查（全部代码符合 SP-CODE-2026-001）
- [ ] 8.2 Python 注释自查（函数体关键逻辑、条件分支、循环、异常处理必须有行内注释）
- [ ] 8.3 后端单元测试自查（每个新增 Python 模块必须有对应 tests/test_*.py，覆盖正向+异常用例）
- [ ] 8.4 运行 `pytest --cov=c302 --cov-report=term-missing` 确认覆盖率 ≥ 90%
- [ ] 8.5 更新 header 索引（`gen_index.py --write`）
- [ ] 8.6 Git 提交并推送
