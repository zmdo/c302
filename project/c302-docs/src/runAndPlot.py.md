# runAndPlot.py 源码分析

> 自动生成于 2026-04-16，基于 commit e9e1100

## 模块概要

`project/c302/runAndPlot.py` 是 c302 的仿真编排入口脚本。
它围绕 `run_c302()` 封装“导入配置模块 → 生成网络 → 调用 jNeuroML 仿真 → 绘制结果图/连接矩阵”的完整执行链，
同时在 `__main__` 中提供一组按配置名和参数层级预置好的 CLI 快捷入口。

除单次运行外，模块还支持 `-all` 批量模式，自动生成汇总 HTML/Markdown 页面并为多组配置输出图像。

## 分析记录

### 2026-04-16 — 阶段五勘误（计划2）

**B 类旗标（注释代码标注）**

- 为 4 处注释代码补充了 `[备选]` 标签：
	- `-fw1` 分支中较短时长的 FW 测试运行参数
	- `-all` 分支中 3 行聚焦少量参数层级的快速批量生成配置

**C 类补充（行内注释增强）**

- 在 `run_c302()` 中补充了关键流程注释：
	- `save_fig_to` 覆盖默认输出目录的用途
	- `config_package` 默认值及 notebooks 扩展配置的导入方式
	- `target_directory` 的职责和创建原因
	- 切换当前工作目录以适配 jNeuroML 文件读写行为
	- 使用 `setup()` 返回的 `nml_doc` 直接生成连接矩阵，避免重复加载

**验证结果**：`gen_index.py` ×2 ✅ → `check_index.py` ✅ → `verify_comment_only.py` ✅（代码等价，仅注释变更）

## 修改记忆

| 日期 | commit | 修改范围 | 修改原因 |
|------|--------|---------|---------|
| 2026-04-16 | 待提交 | B 类标注 ×4，补充 `run_c302()` 行内注释 | 计划2阶段五注释勘误 |

## 踩坑记录

- **坑点**：`-all` 分支内存在两段三引号字符串包裹的旧版 `durations` 配置。
	**原因**：这些内容是字符串字面量而非注释代码，不属于 B 类直接标注范围。
	**解决**：仅对真正的 `# levels = ...` 注释行添加 `[备选]` 标签，保留三引号字符串块不变。
