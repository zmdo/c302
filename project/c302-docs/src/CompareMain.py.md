# CompareMain.py 源码分析

> 自动生成于 2026-04-16，基于 commit e9e1100

## 模块概要

`project/c302/CompareMain.py` 是连接组表格比较工具。
它负责读取两个历史连接组表格文件（XLS / 文本列数据），按连接对进行逐轮匹配，
输出完全匹配、仅存在于某一侧以及方向相反的连接对，主要用于旧版连接组数据核对和格式迁移验证。

模块的核心流程由三部分构成：表格列读取、名称格式化、三轮连接匹配；其中 `matchLists()` 是主要逻辑集中区。

## 分析记录

### 2026-04-16 — 阶段六勘误（计划2）

**B 类旗标（注释代码标注）**

- 为文件中集中保留的旧版/调试代码补充了中文标签，覆盖以下几组内容：
	- `getColumnsXls()` 中按工作表名称读取、读取全部列数、表头打印、逐行调试输出、单元格类型调试
	- `matchLists()` 中旧版列表推导索引实现、中间状态调试输出、反向匹配旧条件和长度检查
	- `typeMapping()` 中未启用的映射表草稿
	- `__main__` 中开发阶段本地绝对路径示例

**实际结果**：计划中预估为 26 处 B 类，审阅后确认主要为多组连续注释代码，已按代码块完成 `[调试]` / `[备选]` / `[废弃]` 标注。

**验证结果**：`gen_index.py` ×2 ✅ → `check_index.py` ✅ → `verify_comment_only.py` ✅（代码等价，仅注释变更）

## 修改记忆

| 日期 | commit | 修改范围 | 修改原因 |
|------|--------|---------|---------|
| 2026-04-16 | 待提交 | 标注 CompareMain.py 中的旧版 XLS 读取与匹配调试代码 | 计划2阶段六注释勘误 |

## 踩坑记录

- **坑点**：文件中有大量独立散落的注释代码，而不是少数连续大块。
	**原因**：旧版 XLS 调试逻辑穿插在现有实现中，若只在函数开头统一加一个标签，语义会覆盖过宽。
	**解决**：按局部代码簇插入 `[调试]` / `[备选]` / `[废弃]` 标签，保持范围精确。
