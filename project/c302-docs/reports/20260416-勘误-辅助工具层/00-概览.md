# 阶段六勘误报告 — 辅助工具层

> 执行日期：2026-04-16  
> 执行计划：计划2 阶段六  
> 覆盖文件：5 个（`CompareMain.py`、`backers.py`、`c302_info.py`、`analyse.py`、`NeuroMLUtilities.py`）

---

## 概览

| 文件 | A 类修正 | B 类标注 | C 类补充 | 备注 |
|------|---------|---------|---------|------|
| `CompareMain.py` | 0 | 多组 | 0 | 集中标注旧版 XLS 读取、匹配调试输出和本地路径示例 |
| `backers.py` | 0 | 0 | 0 | 审阅确认无 A/B/C 问题，仅补齐文档 |
| `c302_info.py` | 0 | 2 | 0 | 标注神经元信息调试输出和旧版 HTML 写出方式 |
| `analyse.py` | 0 | 0 | 0 | 审阅确认无 A/B/C 问题，仅补齐文档 |
| `NeuroMLUtilities.py` | 0 | 1 | 0 | 标注坐标插值调试输出 |
| **合计** | **0** | **多组（至少 3 个文件）** | **0** | — |

---

## 文件级结果

### `CompareMain.py`

- 重点处理旧版 XLS 读取和连接匹配过程中的大量注释代码。
- 已为工作表选择、列数获取、逐行调试输出、旧版匹配索引方案、反向匹配调试、未完成的映射表草稿及本地绝对路径示例补充 `[调试]` / `[备选]` / `[废弃]` 标签。

### `backers.py`

- 审阅全文后确认当前无 A / B / C 类问题。
- 本阶段仅补齐 `.py.md` 分析文档。

### `c302_info.py`

- 已为 `print(ni)` 调试输出补充 `[调试]` 标签。
- 已为旧版 HTML 包装写出方式补充 `[备选]` 标签。
- 实际残留 B 类数量少于计划预估，已按当前源码实际内容执行。

### `analyse.py`

- 审阅全文后确认当前无 A / B / C 类问题。
- 本阶段仅补齐 `.py.md` 分析文档。

### `NeuroMLUtilities.py`

- 已为 `get3DPosition()` 中的旧调试打印补充 `[调试]` 标签。

---

## 验证结果

| 工具 | `CompareMain.py` | `c302_info.py` | `NeuroMLUtilities.py` |
|------|------------------|----------------|------------------------|
| `gen_index.py`（第 1 次） | ✅ | ✅ | ✅ |
| `gen_index.py`（第 2 次） | ✅ | ✅ | ✅ |
| `check_index.py` | ✅ | ✅ | ✅ |
| `verify_comment_only.py` | ✅ | ✅ | ✅ |

> `backers.py` 与 `analyse.py` 本阶段未修改源码，仅补齐文档，因此无需运行源码等价验证。

---

## 结论

阶段六辅助工具层勘误已完成。源码改动仅涉及注释标注，不触碰可执行逻辑；涉及源码的 3 个文件均已完成索引收敛和代码等价验证，5 份对应的 `.py.md` 文档已同步补齐。