# 阶段五勘误报告 — 仿真与可视化层

> 执行日期：2026-04-16  
> 执行计划：计划2 阶段五  
> 覆盖文件：3 个（`c302_utils.py`、`runAndPlot.py`、`gen_graph.py`）

---

## 概览

| 文件 | A 类修正 | B 类标注 | C 类补充 | 备注 |
|------|---------|---------|---------|------|
| `c302_utils.py` | 1 | 约 28 | 1 | 翻译残留英文注释，标注绘图旧方案/调试代码，补充结果绘图主流程注释 |
| `runAndPlot.py` | 0 | 4 | 1 | 标注 CLI 快捷入口中的备选配置，补充 `run_c302()` 执行链路注释 |
| `gen_graph.py` | 0 | 8 | 0 | 标注过滤逻辑与 Graphviz 备选参数/废弃代码 |
| **合计** | **1** | **约 40** | **2** | — |

---

## 文件级结果

### `c302_utils.py`

- A 类：将热图高度计算区域的英文说明改为中文。
- B 类：为旧版矩阵高度估算、旧版 figure 初始化、刻度调试、色图切换、`pcolor()` 渲染、owmeta traceback 输出、旧版单独子图调用等注释代码补充 `[备选]` / `[调试]` 标签。
- C 类：在 `plot_c302_results()` 中补充“结果键名拆分 → 排序 → 矩阵构造 → 字段切换 → 绘图输出”的分段说明。

### `runAndPlot.py`

- B 类：为 `-fw1` 的旧测试参数和 `-all` 分支中的旧参数层级列表补充 `[备选]` 标签。
- C 类：在 `run_c302()` 中补充输出目录覆盖、配置模块导入、工作目录切换和连接矩阵生成来源等说明。

### `gen_graph.py`

- B 类：为 `network` 节点旧写法、过滤肌肉的备选逻辑以及 `concentrate=false` 备选 Graphviz 参数补充 `[备选]` / `[废弃]` 标签。

---

## 验证结果

| 工具 | `c302_utils.py` | `runAndPlot.py` | `gen_graph.py` |
|------|-----------------|-----------------|----------------|
| `gen_index.py`（第 1 次） | ✅ | ✅ | ✅ |
| `gen_index.py`（第 2 次） | ✅ | ✅ | ✅ |
| `check_index.py` | ✅ | ✅ | ✅ |
| `verify_comment_only.py` | ✅ | ✅ | ✅ |

---

## 结论

阶段五源码勘误已完成。三份源码文件仅发生注释层变更，索引已收敛且代码等价验证通过；对应三份 `.py.md` 分析文档已同步补齐。