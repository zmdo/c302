# 阶段四勘误报告 — 网络生成层

> 执行日期：2026-04-16  
> 执行计划：计划2 阶段四  
> 覆盖文件：1 个 `__init__.py`（2172 行）

---

## 概览

| 文件 | A 类修正 | B 类标注 | C 类补充 | 备注 |
|------|---------|---------|---------|------|
| `__init__.py` | 3 | 20 | 0 | 方法索引英文描述翻译；注释代码全量标注 |
| **合计** | **3** | **20** | **0** | — |

---

## A 类修正详情

方法索引中遗留 3 条英文描述，统一翻译为中文：

| 方法名 | 旧描述（英文） | 新描述（中文） |
|--------|---------------|---------------|
| `load_data_reader` | `Imports and returns data reader module` | `动态导入并返回指定名称的数据读取器模块` |
| `get_str_from_exponential` | `Returns a formatted string representing a floating point number, e.g. 1*0.00001 would result into 1e-05. Returning 0.00001.` | `将浮点数格式化为字符串表示（如 0.00001 → "1e-05"）` |
| `process_args` | `Parse command-line arguments.` | `解析 CLI 命令行参数，返回 argparse.Namespace 对象` |

---

## B 类标注详情

共为 20 处注释代码行（组）添加 `[调试]`/`[备选]`/`[废弃]` 标签前缀。

### `[备选]` × 9

| 位置 | 内容 | 说明 |
|------|------|------|
| 约 L72 | `# import c302.bioparameters` | 可切换为本地 bioparameters 模块 |
| 约 L110–113 | `# DEFAULT_DATA_READER = "SpreadsheetDataReader"` 等 3 行 | 旧版备选数据读取器路径 |
| 约 L408 | `# cell_file_path = root_dir + "/../../../"...` | 测试模式下的备选路径 |
| 约 L461 | `# phase = get_cell_position(cell).x` | 从 soma x 坐标推算相位的备选实现 |
| 约 L1091 | `# lems_file = ""` | 旧版字符串形式 LEMS 文件变量 |
| 约 L1122 | `# def_file = './%s' % ctd` | 使用相对路径的备选写法 |
| 约 L1556 | `# if elem_in_coll_matches_conn(...)` | 旧版极性覆盖逻辑 |
| 约 L1604 | `# number_syns = conn.number*conn_number_scaling[...]` | 旧版缩放（不支持正则） |
| 约 L1775–1776 | `# if conn.post_cell in lems_info['cells']:` + `#    conn_type = "muscle_to_neuron"` | 旧版连接类型判断 |

### `[调试]` × 10

| 位置 | 内容 | 说明 |
|------|------|------|
| 约 L411 | `# print "%s, %s, %s" %(location.x, ...)` | soma 坐标打印 |
| 约 L775 | `# print('------ Getting the cell info for %s'%cells)` | owmeta 查询输入打印 |
| 约 L808 | `# print_("No matching cell for %s" % name)` | 未匹配细胞警告 |
| 约 L854 | `# print('==== Returning %s; %s'%(...))` | 函数返回值检查 |
| 约 L1370 | `# print_('Positioning muscle: %s at (%s,%s,%s)')` | 肌肉坐标结果 |
| 约 L1591 | `# print("Scaling by %s"%scale)` （神经元段） | 缩放因子（神经元连接） |
| 约 L1669–1671 | `# print "######## ..." + 3 行` | 运动神经元连接调试输出 |
| 约 L1691 | `# print_("Conn %s -> %s"%(pre_cell_id,post_cell_id))` （神经元段） | 神经元间 cell_id 路径 |
| 约 L1897 | `# print("Scaling by %s"%scale)` （肌肉段） | 缩放因子（肌肉连接） |
| 约 L1973 | `# print_("Conn %s -> %s"%(pre_cell_id,post_cell_id))` （肌肉段） | 神经肌肉 cell_id 路径 |
| 约 L2039 | `# import pprint` + `# pprint.pprint(lems_info)` | lems_info 字典完整打印 |

### `[废弃]` × 1

| 位置 | 内容 | 说明 |
|------|------|------|
| 约 L1373 | `# target = "%s/0/%s"...(# unused)` | 已明确标注 unused 的旧 target 字符串 |

---

## 验证结果

| 工具 | 结果 |
|------|------|
| `gen_index.py`（第 1 次） | ✅ 索引已更新 |
| `gen_index.py`（第 2 次） | ✅ 索引已稳定 |
| `check_index.py` | ✅ 所有索引与实际代码一致 |
| `verify_comment_only.py` | ✅ 代码等价，仅注释变更 |
